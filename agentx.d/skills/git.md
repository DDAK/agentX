# Git workflow

When asked to commit:
- Run `git status` and `git diff` first to review changes.
- Write a concise, imperative commit message ("Add X", not "Added X").
- Never commit secrets or `.env` files.
